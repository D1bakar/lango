/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Do not advertise the framework and version in responses.
  poweredByHeader: false,
};

export default nextConfig;
